from sklearn.decomposition import PCA
import matplotlib.pyplot as plt
import numpy as np

# Assuming wt_vec, mut1_vec, and mut2_vec are already defined

# Combine the k-mer vectors into a single array
# Each row represents a sample, and each column represents a k-mer frequency
X = np.array([wt_vec, mut1_vec, mut2_vec])

# Initialize and fit PCA
# We'll reduce the data to 2 principal components for visualization
pca = PCA(n_components=2)
X_pca = pca.fit_transform(X)

# X_pca now contains the 2D coordinates for each sample (WT, Mut1, Mut2)

# Visualize the results
plt.figure(figsize=(8, 6))
plt.scatter(X_pca[:, 0], X_pca[:, 1], c=['black', 'green', 'orange'], s=100) # s is marker size

# Add labels for each point
labels = ["WT", "Mutant 1", "Mutant 2"]
for i, label in enumerate(labels):
    plt.text(X_pca[i, 0] + 0.01, X_pca[i, 1], label, fontsize=12) # Adjust text position

plt.xlabel("PC1")
plt.ylabel("PC2")
plt.title("PCA of K-mer Frequency Vectors")
plt.grid(True)
plt.show()
